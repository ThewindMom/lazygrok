---
description: >
  Ultrawork / ulw — one keyword is enough. Activates LazyGrok ULW mode: host goal
  tools when available, # Goal transcript contract, ulw-loop evidence ledger,
  todo_write, RED→GREEN→SURFACE proof. You do not need /goal separately.
---

# Ultrawork

The user said **ulw** or **ultrawork** (or invoked this command). That is sufficient.

1. First line: `ULTRAWORK MODE ENABLED!`
2. **read_file** skill `ultrawork` (full directive) and follow it.
3. Register goals yourself (silent on missing tools):
   - `create_goal` / `update_goal` if in the tool list
   - always `# Goal` markdown + ulw-loop `create-goals` (skill `ulw-evidence`)
4. Execute with evidence; LIGHT finish with `light-quality-gate` + `checkpoint` when using the ledger.

Do **not** ask the user to type `/goal` or other slash commands unless they explicitly want host goal UI controls (`status` / `pause` / `clear`).
